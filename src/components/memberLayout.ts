import {k, w, g, n} from '../data/memberData'

function Membro({nome, idade, imgPos}: membroProps){
    return(
        <>
            {imgPos === 'left' ? () : ()}
        </>
    )
}

<Membro {...k}/>


